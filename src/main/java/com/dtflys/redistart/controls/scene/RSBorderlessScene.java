package com.dtflys.redistart.controls.scene;

import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;

public class RSBorderlessScene extends Scene {

    /** The root. */
    private Parent root;

    /** The Stage. */
    private Stage stage;

    public RSBorderlessScene(Parent parent, double v, double v1) {
        super(parent, v, v1);
    }
}
